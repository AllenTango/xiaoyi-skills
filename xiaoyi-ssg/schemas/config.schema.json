{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "xiaoyi-ssg site config",
  "type": "object",
  "required": ["site"],
  "properties": {
    "site": {
      "type": "object",
      "required": ["title", "language"],
      "properties": {
        "title": { "type": "string" },
        "subtitle": { "type": "string" },
        "author": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "language": { "type": "string", "pattern": "^[a-z]{2,3}(-[A-Z]{2})?$" },
        "timezone": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "description": { "type": "string" },
        "design": {
          "type": "object",
          "properties": {
            "brand": { "type": "string" },
            "source_skill": { "type": "string" }
          }
        },
        "build": {
          "type": "object",
          "description": "Build-time overrides. Default output_dir is `public/`. When a user is migrating from an existing static site generator (Hugo / Jekyll / Astro / Next-static-export / etc.) whose `public/` / `_site/` / `dist/` / `out/` directory already exists, the AI must use `.xiaoyi-ssg-build/` instead to avoid collision. The renderer does not overwrite an existing non-empty output directory.",
          "properties": {
            "per_page": { "type": "number", "default": 10 },
            "output_dir": { "type": "string", "default": "public" }
          }
        }
      }
    },
    "pages": { "type": "array" },
    "nav": { "type": "array" },
    "social": { "type": "array" },
    "geo": {
      "type": "object",
      "description": "GEO / AI-bots settings. All fields are optional with engine defaults. Full spec: prompts/geo-conventions.md.",
      "properties": {
        "ai_bots": { "type": "string", "enum": ["allow", "block", "custom"], "default": "allow" },
        "ai_bot_rules": {
          "type": "object",
          "description": "Per-bot allow/block map; only used when ai_bots: 'custom'. Keys are bot names (must be a subset of the canonical 15-bot list, validated at runtime by validateConfig), values are 'allow' or 'block'.",
          "additionalProperties": { "type": "string", "enum": ["allow", "block"] }
        },
        "llms_full":       { "type": "boolean", "default": false,  "description": "Emit /llms-full.txt. Opt-in; default off." },
        "markdown_mirror": { "type": "boolean", "default": true,   "description": "Emit per-page /<url>/index.md alongside the HTML." },
        "jsonld":          { "type": "boolean", "default": true,   "description": "Inject JSON-LD structured data in <head>." },
        "noai":            { "type": "boolean", "default": false,  "description": "Site-wide default for <meta name='robots' content='noai'>. Per-page frontmatter overrides this." }
      },
      "additionalProperties": false
    },
    "dev": {
      "type": "object",
      "description": "Dev-server settings read by .xiaoyi-ssg/dev.js. The `port` field drives the mandatory port-auto-increment pattern (see prompts/render-node-spec.md).",
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 3000,
          "description": "Base port for the dev server. dev.js increments on EADDRINUSE (max 20 attempts) before exiting non-zero."
        },
        "host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind address. Default loopback only; switch to 0.0.0.0 explicitly when needed for LAN preview."
        }
      },
      "additionalProperties": false
    }
  }
}
